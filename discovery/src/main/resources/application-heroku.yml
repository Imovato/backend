spring:
  config:
    activate:
      on-profile: heroku
  application:
    name: discovery

server:
  port: ${PORT:8080}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
